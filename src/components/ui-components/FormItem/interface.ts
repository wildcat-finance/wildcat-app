export type InputProps =  {
    label?: React.ReactNode,
    hint?: string,
    endDecorator?: React.ReactNode
    className?: string,
    tooltip?: string,
    error?: boolean;       
    errorText?: string;
    children?: React.ReactNode
}